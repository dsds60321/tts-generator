---
phase: 01-main-py
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - requirements.txt
  - tests/test_main.py
autonomous: true
requirements:
  - API-03
  - SYN-01
  - SYN-02
  - SYN-03
must_haves:
  truths:
    - "실행자는 `/synthesize` 공개 계약이 `text`, `voice`, `speed`라는 것을 코드와 테스트에서 바로 확인할 수 있다"
    - "실행자는 지원 화자 allowlist와 속도 변환 규칙을 `main.py` 계약으로 확인할 수 있다"
    - "실행자는 성공 응답과 provider/FFmpeg 실패 응답을 자동 검증할 테스트 토대를 가진다"
  artifacts:
    - path: "main.py"
      provides: "FastAPI 앱, 요청 모델, 화자 allowlist, 속도 변환 함수 시그니처, `/synthesize` route 계약"
      contains: "class SynthesizeRequest"
    - path: "requirements.txt"
      provides: "프로토타입 실행과 테스트에 필요한 고정 버전 의존성"
      contains: "edge-tts==7.2.7"
    - path: "tests/test_main.py"
      provides: "WAV 응답 계약과 helper 호출을 검증하는 pytest 스모크 테스트"
      contains: "def test_synthesize_returns_wav_file_response"
  key_links:
    - from: "tests/test_main.py"
      to: "main.py"
      via: "TestClient(app) import와 helper monkeypatch"
      pattern: "from main import app"
    - from: "main.py"
      to: "tests/test_main.py"
      via: "to_edge_rate와 `/synthesize` route 공개 계약"
      pattern: "def to_edge_rate"
---

<objective>
`/synthesize` 프로토타입을 구현하기 전에 실행 계약과 테스트 토대를 고정한다.

Purpose: Phase 1 범위를 벗어나지 않고 `main.py` 단일 파일 중심 프로토타입이 따라야 할 공개 계약과 자동 검증 경계를 먼저 확정한다.
Output: `main.py` 계약 스켈레톤, 고정 버전 `requirements.txt`, `/synthesize` 테스트 스캐폴드
</objective>

<execution_context>
@/Users/kanggeonho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kanggeonho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-main-py/01-RESEARCH.md
@.planning/phases/01-main-py/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `main.py` 공개 계약 스켈레톤 작성</name>
  <files>main.py</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-main-py/01-RESEARCH.md
    - .planning/phases/01-main-py/01-VALIDATION.md
    - main.py
  </read_first>
  <action>`main.py`를 새로 만들고 단일 파일 중심 구조를 유지한다. `FastAPI` 앱 인스턴스 `app = FastAPI(...)`, 정적 화자 allowlist `SUPPORTED_VOICES = {"SunHi": "ko-KR-SunHiNeural", "InJoon": "ko-KR-InJoonNeural", "JiMin": "ko-KR-JiMinNeural", "BongJin": "ko-KR-BongJinNeural"}`, 요청 모델 `class SynthesizeRequest(BaseModel)` with `text`, `voice`, `speed`, 속도 어댑터 시그니처 `def to_edge_rate(speed: float) -> str`, provider helper `async def synthesize_to_mp3(...)`, FFmpeg helper `async def convert_mp3_to_wav(...)`, cleanup helper, `@app.post("/synthesize")` route 시그니처를 먼저 고정한다. 이 단계에서는 `/health`, `/voices`, 웹 UI, provider 추상화 디렉터리, 추가 모듈 분리는 만들지 않는다.</action>
  <acceptance_criteria>
    - `rg "SUPPORTED_VOICES = \\{" main.py`가 매치된다
    - `rg "class SynthesizeRequest" main.py`가 매치된다
    - `rg "def to_edge_rate\\(speed: float\\) -> str" main.py`가 매치된다
    - `rg "@app.post\\(\"/synthesize\"\\)" main.py`가 매치된다
    - `python -m py_compile main.py`가 종료 코드 0을 반환한다
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile main.py && rg 'SUPPORTED_VOICES = \{' main.py && rg 'class SynthesizeRequest' main.py && rg '@app.post\("/synthesize"\)' main.py</automated>
  </verify>
  <done>`main.py`만 읽어도 공개 입력 필드, 허용 화자 집합, 속도 변환 책임, `/synthesize` route 존재 여부를 이해할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 고정 의존성과 `/synthesize` 테스트 토대 작성</name>
  <files>requirements.txt, tests/test_main.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-main-py/01-RESEARCH.md
    - .planning/phases/01-main-py/01-VALIDATION.md
    - main.py
    - requirements.txt
    - tests/test_main.py
  </read_first>
  <action>`requirements.txt`를 새로 만들고 `fastapi[standard]==0.135.1`, `uvicorn==0.42.0`, `edge-tts==7.2.7`, `pytest==9.0.2`, `httpx==0.28.1`를 고정한다. `tests/test_main.py`를 만들고 `TestClient(app)` 기반 테스트를 작성한다. 테스트 이름은 최소한 `test_synthesize_returns_wav_file_response`, `test_synthesize_passes_text_to_provider`, `test_synthesize_passes_selected_voice`, `test_speed_maps_to_edge_rate`, `test_synthesize_returns_debuggable_500_when_provider_fails`, `test_synthesize_returns_debuggable_500_when_ffmpeg_fails`를 사용한다. 실제 네트워크와 `ffmpeg` 호출은 monkeypatch로 대체하고, monkeypatch 대상은 `main.py`의 helper 이름에 맞춘다. 이 단계에서는 테스트를 녹색으로 만들 필요는 없지만, 다음 plan이 구현할 계약을 해석 없이 따라갈 수 있을 정도로 구체적으로 적는다.</action>
  <acceptance_criteria>
    - `rg "^fastapi\\[standard\\]==0\\.135\\.1$" requirements.txt`가 매치된다
    - `rg "^edge-tts==7\\.2\\.7$" requirements.txt`가 매치된다
    - `rg "def test_synthesize_returns_wav_file_response" tests/test_main.py`가 매치된다
    - `rg "def test_speed_maps_to_edge_rate" tests/test_main.py`가 매치된다
    - `rg "provider_fails" tests/test_main.py`와 `rg "ffmpeg_fails" tests/test_main.py`가 각각 매치된다
    - `rg "monkeypatch" tests/test_main.py`가 매치된다
  </acceptance_criteria>
  <verify>
    <automated>mkdir -p tests && python -m py_compile main.py && rg '^fastapi\[standard\]==0\.135\.1$' requirements.txt && rg '^edge-tts==7\.2\.7$' requirements.txt && rg 'def test_synthesize_returns_wav_file_response' tests/test_main.py && rg 'def test_synthesize_returns_debuggable_500_when_ffmpeg_fails' tests/test_main.py</automated>
  </verify>
  <done>구현 plan이 참고할 의존성 고정값과 `/synthesize` 계약 테스트 이름, monkeypatch 지점, 실패 시나리오가 파일로 고정되어 있다.</done>
</task>

</tasks>

<verification>
Plan 완료 시 `python -m py_compile main.py`가 성공하고, `requirements.txt`와 `tests/test_main.py`에 다음 plan 구현을 구속하는 고정 문자열과 테스트 이름이 존재해야 한다.
</verification>

<success_criteria>
다음 plan 실행자는 리서치 문서를 다시 해석하지 않아도 `main.py`와 `tests/test_main.py`만 읽고 `/synthesize` 구현 계약을 바로 수행할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-py/01-main-py-01-SUMMARY.md`
</output>
