---
phase: 01-main-py
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - main.py
  - README.md
autonomous: true
requirements:
  - API-03
  - SYN-01
  - SYN-02
  - SYN-03
must_haves:
  truths:
    - "API 사용자는 한국어 `text`, 정적 allowlist `voice`, float `speed`로 `/synthesize`를 호출할 수 있다"
    - "성공 요청은 즉시 다운로드 가능한 `audio/wav` 응답을 반환한다"
    - "선택한 화자와 속도가 provider 호출에 반영된다"
    - "provider 네트워크 실패나 FFmpeg 실패는 원인 추적 가능한 `5xx` 서버 오류로 드러난다"
  artifacts:
    - path: "main.py"
      provides: "`edge-tts` MP3 생성, FFmpeg WAV 변환, FileResponse 반환, 실패 처리"
      contains: "asyncio.create_subprocess_exec"
    - path: "README.md"
      provides: "설치, 실행, `/synthesize` 호출 예제, FFmpeg 전제조건, 수동 스모크 체크 절차"
      contains: "curl -X POST"
  key_links:
    - from: "main.py"
      to: "edge_tts.Communicate"
      via: "synthesize_to_mp3 helper"
      pattern: "edge_tts\\.Communicate"
    - from: "main.py"
      to: "ffmpeg"
      via: "convert_mp3_to_wav helper"
      pattern: "asyncio\\.create_subprocess_exec"
    - from: "main.py"
      to: "WAV HTTP response"
      via: "FileResponse with BackgroundTask cleanup"
      pattern: "FileResponse\\("
---

<objective>
`/synthesize` 핵심 흐름을 실제로 동작시키고, 같은 phase 안에서 바로 재현 가능한 실행 문서를 남긴다.

Purpose: Phase 1의 목표인 "한국어 텍스트 -> 선택 화자/속도 반영 -> `WAV` 즉시 반환"을 `main.py` 최소 구조에서 끝낸다.
Output: 동작하는 `main.py` synthesize pipeline, 실행/검증 지침이 들어간 `README.md`
</objective>

<execution_context>
@/Users/kanggeonho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kanggeonho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-main-py/01-RESEARCH.md
@.planning/phases/01-main-py/01-VALIDATION.md

<interfaces>
From `main.py` contract created in `01-01`:
```python
SUPPORTED_VOICES = {
    "SunHi": "ko-KR-SunHiNeural",
    "InJoon": "ko-KR-InJoonNeural",
    "JiMin": "ko-KR-JiMinNeural",
    "BongJin": "ko-KR-BongJinNeural",
}

class SynthesizeRequest(BaseModel):
    text: str
    voice: str
    speed: float = 1.0

def to_edge_rate(speed: float) -> str: ...
async def synthesize_to_mp3(...) -> None: ...
async def convert_mp3_to_wav(...) -> None: ...
@app.post("/synthesize")
async def synthesize(request: SynthesizeRequest): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `edge-tts` + `FFmpeg` 기반 `/synthesize` 구현</name>
  <files>main.py</files>
  <read_first>
    - .planning/phases/01-main-py/01-RESEARCH.md
    - .planning/phases/01-main-py/01-VALIDATION.md
    - .planning/phases/01-main-py/01-main-py-01-SUMMARY.md
    - main.py
    - tests/test_main.py
  </read_first>
  <behavior>
    - Test 1: `POST /synthesize` with JSON `{ "text": "안녕하세요", "voice": "SunHi", "speed": 1.0 }` returns status `200`, `content-type` containing `audio/wav`, and `content-disposition` containing `speech.wav`
    - Test 2: provider helper receives the original `text` string and the mapped edge voice for the selected `voice`
    - Test 3: `to_edge_rate(1.25)` produces `+25%` and `to_edge_rate(0.8)` produces `-20%`
    - Test 4: provider/network failure and FFmpeg failure both return debuggable `500` responses instead of corrupt files
  </behavior>
  <action>`main.py`의 스켈레톤을 실제 구현으로 채운다. 공개 요청 필드는 `text`, `voice`, `speed`만 유지하고, 화자는 `SunHi`, `InJoon`, `JiMin`, `BongJin` 정적 allowlist만 허용한다. `speed`는 공개적으로 float multiplier를 유지하되 `to_edge_rate()`에서 `round((speed - 1.0) * 100)` 계산으로 `edge-tts` rate 문자열(`+25%`, `-20%`)로 변환한다. provider 호출은 `edge_tts.Communicate(text, mapped_voice, rate=rate).save(str(mp3_path))`로 MP3를 만들고, 변환은 `asyncio.create_subprocess_exec("ffmpeg", "-y", "-i", str(mp3_path), "-ar", "24000", "-ac", "1", str(wav_path), ...)`를 사용한다. 임시 파일은 `tempfile.mkdtemp(prefix="tts-")`에 두고, 성공 시 `FileResponse(..., media_type="audio/wav", filename="speech.wav", background=BackgroundTask(shutil.rmtree, temp_dir, ignore_errors=True))`로 반환한다. provider/network 예외와 FFmpeg 종료 코드 실패는 원인 문자열을 남긴 `HTTPException(status_code=500, detail=...)` 또는 동등한 JSON 오류로 감싸되, `/health`, `/voices`, HTML 응답, provider 추상화 계층은 추가하지 않는다.</action>
  <acceptance_criteria>
    - `rg "edge_tts\\.Communicate" main.py`가 매치된다
    - `rg "asyncio\\.create_subprocess_exec" main.py`가 매치된다
    - `rg "FileResponse\\(" main.py`와 `rg "BackgroundTask\\(" main.py`가 각각 매치된다
    - `rg "audio/wav" main.py`가 매치된다
    - `python -m pytest tests/test_main.py -q`가 종료 코드 0을 반환한다
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main.py -q</automated>
  </verify>
  <done>`main.py` 하나로 한국어 텍스트, 선택 화자, 속도를 받아 `WAV`를 즉시 반환하는 최소 프로토타입과 디버깅 가능한 5xx 실패 경로가 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: 실행과 수동 스모크 절차를 `README.md`에 고정</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - requirements.txt
    - main.py
    - .planning/phases/01-main-py/01-RESEARCH.md
  </read_first>
  <action>placeholder `README.md`를 한국어 runbook으로 교체한다. 반드시 포함할 항목은 `python3 -m venv .venv`, `source .venv/bin/activate`, `pip install -r requirements.txt`, `ffmpeg -version`, `uvicorn main:app --reload`, `POST /synthesize` sample `curl` with JSON fields `text`, `voice`, `speed`, 지원 화자 목록 `SunHi`, `InJoon`, `JiMin`, `BongJin`, 그리고 provider/network failure 또는 FFmpeg failure가 `5xx`로 보일 수 있다는 운영 메모다. README는 이번 phase 범위가 `/synthesize` 핵심 흐름뿐이며 `/health`, `/voices`, 웹 UI는 다음 phase라고 명시한다.</action>
  <acceptance_criteria>
    - `rg "uvicorn main:app --reload" README.md`가 매치된다
    - `rg "ffmpeg -version" README.md`가 매치된다
    - `rg "curl -X POST" README.md`가 매치된다
    - `rg "SunHi" README.md`와 `rg "BongJin" README.md`가 각각 매치된다
    - `rg "/health" README.md`와 `rg "/voices" README.md`가 각각 매치되어 후속 phase 범위를 설명한다
  </acceptance_criteria>
  <verify>
    <automated>rg 'uvicorn main:app --reload' README.md && rg 'ffmpeg -version' README.md && rg 'curl -X POST' README.md && rg 'SunHi' README.md && python -m pytest -q</automated>
  </verify>
  <done>저장소만 받아도 의존성 설치, 서버 실행, `/synthesize` 호출, 수동 smoke 검증, 알려진 실패 surface를 README만으로 재현할 수 있다.</done>
</task>

</tasks>

<verification>
전체 phase 검증은 `python -m pytest -q` 통과와 README 절차대로 `POST /synthesize` 호출 시 `speech.wav`가 내려오는 수동 smoke 1회를 기준으로 한다.
</verification>

<success_criteria>
Phase 1 완료 후 저장소에는 `requirements.txt`, `main.py`, `tests/test_main.py`, `README.md`가 존재하고, `/synthesize`가 `text`, `voice`, `speed`를 받아 `WAV`를 반환하는 최소 프로토타입이 자동 테스트로 보호된다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-py/01-main-py-02-SUMMARY.md`
</output>
